#!/bin/sh
# 推送前刷新 codegraph 索引(保持与远端基线一致)。codegraph 未装则静默跳过,绝不阻断 push。
# 智能选测:`codegraph affected <changed-files>` 可圈出受影响测试只重跑(见 docs/CP-009 §5),
# 此处不强制跑测试以免阻断 push。
command -v codegraph >/dev/null 2>&1 || exit 0
codegraph sync . >/dev/null 2>&1 || true
exit 0
